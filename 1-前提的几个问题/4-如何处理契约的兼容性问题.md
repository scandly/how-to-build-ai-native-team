如何处理契约的兼容性问题（概要版）

兼容性必须是数学意义上的严格兼容，由机器规则和消费者测试三层防线保障。

---

### 三层防线
- **兼容性规则引擎**：只允许可逆变更（新增可选字段、拓宽数值范围、枚举新增值）；禁止破坏性操作（删字段、改名、改类型、必填改选填），违规直接拦截。
- **消费者驱动契约测试（CDCT）**：消费方AI出题上传测试池，提供方每次提交跑所有消费者的测试用例，任一失败则部署终止。
- **运行时网关校验**：网关加载最新Schema进行强类型校验，不合规响应直接切断并触发告警，保护消费方不受有毒数据影响。

**核心**：不兼容的代码根本运行不起来。
